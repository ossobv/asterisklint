[general]
clearglobalvars=yes

[globals]
SOMEGLOBAL=X
SOMEGLOBAL2=Y

[test]
exten => s,1,NoOp
 same => n,Set(a=)
 same => n,NoOp(NIET GOED)
 same => n,NoOp($[${a}=])
 same => n,NoOp($[${a}=""])
 same => n,NoOp($[="${a}"])
 same => n,NoOp($[=${a}])

 same => n,NoOp(WEL GOED)
 same => n,NoOp($["${a}"=""])

 same => n,Set(a=x)
 same => n,NoOp(OOK GOED)
 same => n,NoOp($[${a}=x])
 same => n,NoOp($[${a}="x"])
 same => n,NoOp($["${a}"=x])
 same => n,NoOp($["${a}"="x"])
